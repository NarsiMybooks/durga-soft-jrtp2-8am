spring:
  application:
    name: weshopify-service-registry
    
server: 
     port: 8761
    
eureka:
  client:
    register-with-eureka: false
    fetch-registry: true
    
  instance:
    hostname:  ${REGISTRY_HOST_NAME:localhost}
    prefer-ip-address: true
    
    
